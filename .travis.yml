language: go
sudo: false

matrix:
  include:
    - go: 1.7
    - go: tip
  allow_failures:
    - go: tip

script:
  - diff -u <(echo -n) <(gofmt -d .)
  - go vet $(go list ./... | grep -v /vendor/)
  - go test -v -race ./...
